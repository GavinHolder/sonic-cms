# Live-derived fixtures contain a real site's content — fetched on demand, never committed.
fixtures/live/
# Screenshots, per-run JSON results and rect dumps.
test-artifacts/
# Desktop-bg baseline numbers captured from live-derived fixtures (see run.ts --capture-baseline).
baselines/*.live.json
